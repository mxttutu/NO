#include <iostream>


int main()
{
    std::cout << "Hello World!\n";
    std::cout << "these are commmand line arguments, do not use Capitalisation\n";
    std::cout << "type / error.make to make a custom js error";
    std::cout << "type /runtime.collapse to end runtime\n";
    std::cout << "type /connect.server to connect to the server\n";
    std::cout << "type /load.server download the html value from the web\n";
    std::cout << "type /clone.website to clone it\n";
    std::cout << "type /build.localhost to launch the code\n";
    std::cout << "type /gui to open a little GUI page type  /gui.ssl to open a little GUI page for ssl gen\n";}



 
